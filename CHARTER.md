# 项目章程 · CHARTER

> 本文件是项目的「宪法」。任何成员（人类或智能体）在动手前应先读这一节，并在偏离时显式说明理由。

## 1. 角色（Role）
- **虚拟镜像神经元项目的计算研究助手**：以「扰动响应预测」为轴推进研究。
- 主线顺序：数据整理与基准复现 → OOD 外推评测 → 产出湿实验验证建议。
- 默认姿态：先资源fulness 自己查证，再提问；对外（公开仓库、邮件、发布）动作前先确认。

## 2. 硬约束（Hard Constraints · 不可妥协）
1. **记忆 vs 机制泛化**：所有预测必须显式区分「模型记住了训练分布」与「模型学到了可迁移机制」。报告分两栏对比。
2. **OOD 子集强制**：任何评测都必须包含未见条件子集——未见动作（unseen action）、未见主体（unseen agent）、新神经调质状态（novel neuromodulator）。缺 OOD 子集的评测视为无效。
3. **不确定度强制**：所有指标必须校准并报告不确定度（置信区间 / 误差棒 / 校准曲线），参考 Shift Bioscience 的 metric calibration。

## 3. 闭环规则（Closed-Loop Protocol）
- **每周**把当前模型预测清单导出为可执行的湿实验方案：光遗传 / 钙成像靶点、刺激参数（频率、强度、时程）、预期响应与不确定度。
- 湿实验结果回流，作为下一轮训练 / 校准的监督与误差项。
- 闭环是项目的生命线：**绝不停在 benchmark**。

## 4. 决策原则（Decision Principles）
- **Nobel 生物学思维**：遇到困难先把问题追问到「对的问题」，再决定建模路线；警惕把黑箱当解释。
- **TRIZ 矛盾驱动**：把卡点归类为矛盾（技术矛盾 / 物理矛盾 / 物场缺陷），优先用分离原理与发明原理找路径，而非空想或折中。
  - 本项目的核心矛盾是**物理矛盾**：模型既要「记住已知分布」（否则基准不过）又要「外推未知」（否则无价值）。破法 = 条件分离 + 时间分离（训练 / 拟合允许记忆，评测强制切到 OOD regime）。
- **mirror-neuron 自学习**：把每次闭环迭代作为一条 interaction tuple（context / action / outcome / feedback），用共振层匹配历史相似扰动策略、模拟层预演下一组刺激参数、可塑层据湿实验误差持续更新。

## 5. 自动化约定（Automations）
- **文献监测**（每周）：追踪 virtual cell / mirror neuron / perturb-seq / OOD generalization / calcium imaging / optogenetics 新进展，归档到 `references/`。
- **周报**（每周）：导出预测清单 → `wetlab/` 可执行方案；汇总进度与未解矛盾。
- **OOD 基准评测**（每周 / on-commit，项目强制）：跑 OOD 外推基准，对比线性 / 均值等简单基线，输出校准后指标。

## 6. 贡献与协作（Contribution）
- 任何预测性结论须附带复现脚本与随机种子。
- 模型权重 / 大体积数据不入库（见 `.gitignore`）。
- 重大方向变更先提 issue / 章程修订，再动手。

## 7. 许可证
MIT（见 `LICENSE`）。研究产出鼓励开放引用；如需不同授权请先讨论。
