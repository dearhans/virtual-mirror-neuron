# 虚拟镜像神经元 · 周报 2026-W33

- **报告版本**：**2026-08-16 刷新（第 6 版）** —— 取代 08-10 首版 / 08-12 二版 / 08-14 三、四、五版；同 ISO 周第 6 次导出
- **种子基准**：`experiments/20260816-benchmark.json`（真实 Norman 2019，UTC 08-16 08:53 跑批产出；与 `20260812-benchmark.json` 三段 sha256 逐位冻结）
- **样本规模**：train 55464 / ID-test 11953 / total 99289；bootstrap = 100
- **本周新证据槽（本轮）**：**P5-A2 判据审计 —— 08-15「合成数据四假说全过」重裁为「预锁判据不通过」**（判据漂移，非模型失败）
- **配套湿实验方案**：`wetlab/2026-W33.md`（7 协议 E1–E7 + §6 人工附录五小节）
- **状态**：未外发、未发布、未向 GOAI 提交
- **⚠️ 外部节点**：**今日（2026-08-16）为 GOAI 虚拟细胞赛道初赛截止日**。提交包 `submissions/GOAI初赛_CompositionalTwin_OOD外推.zip`（92.98 MB，08-07 打包）在位但**未提交**；GOAI 侧自 08-08 起无新证据落盘。

---

## 0. 本周口径声明（先说没做什么）

1. **W37 主基准跑批已产出（UTC 08-16 08:53），结果与 `20260812` 逐位冻结——属确定性复现，非新进展。**
   `experiments/20260816-benchmark.json`（40765 字节）已生成；`experiments/w37_benchmark_run_20260816.log` 现 **839 字节**（此前 0 字节空文件已被本次跑批覆盖）。
   `20260816-regress-check.json`：`curr_results_sha256 = prev_results_sha256 = 47f8fcd9…3def663d` → **results/calibration/flags 三段 sha256 与 20260812 全等**。
   → 本周报与 wetlab 方案的**全部数值**来自 `20260816-benchmark.json`（与 `20260812` 数值逐位一致），**无新增建模增量**。
   这是「结果稳定（冻结）」，不是「跑批未落地」——与早先 0 字节 log 时的误判相反，此处显式更正。

2. **主基准连续多周零建模增量（sha256 三段逐位冻结，本次为第 6 次）。**
   `20260816-regress-check.json`：`results_frozen=true`、`calibration_frozen=true`、`flags_frozen=true`、
   `n_results_diff=0`、`n_calibration_diff=0`。`prev=20260812`、`curr=20260816`，
   `curr_results_sha256 = prev_results_sha256 = 47f8fcd9…3def663d`（calibration/flags 同）。
   → 主基准维度属**确定性复现**，任何「模型变好了」的表述都不成立。

3. **本周唯一实质新增是一条方法论层面的自我证伪**（§3）：
   08-15 记录的「P5-A2 合成数据四假说全过（阳性）」在**预锁判据**下重裁为**不通过**。
   失败点不在模型，而在**判据被软化**（criterion drift）——这比「模型不行」更需要立即纠正。

4. **P5 四机制现状未变**：P5-A(SGLD)/P5-B(NCL)/P5-C(GMM) 在真实 Norman 基准全部阴性；
   P5-A2(proper-Bayesian) **仅有合成数据结果，且本周重裁为判据不通过**。
   → **R4 兜底继续生效**：OOD 区间标「不可信」，仅点估计参与机制泛化判决。

5. **本周报零引用未落盘工件。** §3 的重裁结论**全部**可从已落盘的
   `experiments/20260815-p5a2-spike.json`（1659 字节）直接复算，不依赖任何在跑任务。
   （自动化审计脚本 `scripts/p5a2_readjudicate.py` 因 PyMC NUTS 编译需 g++ 而未产出其 JSON，
   §3 重裁由手工算术完成，与脚本逻辑一致——0.2125 与 0.0500 为 `mean(|1−L|)` 的算术恒等结果。）

---

## 1. 双栏总表 · 记忆（in-distribution） vs 机制泛化（OOD）

RMSE（bootstrap 均值）与 95% CI。⚠️ 字段口径：`results[*].rmse` 是 **bootstrap 均值**，非点估计。

### 左栏 · 记忆（ID，已见基因 n=11953）

| 排名 | 预测器 | RMSE [95% CI] | R² | ECE | 覆盖@0.9 |
|---|---|---|---|---|---|
| 1 | `linear` | **0.5664** [0.5608, 0.5712] | 0.7696 | 0.0318 | 0.915 |
| 2 | `virtual_twin` | 0.5671 [0.5615, 0.5717] | 0.7691 | **0.7216** | **0.078** |
| 3 | `compositional_twin` | 0.5673 [0.5617, 0.5717] | 0.7690 | 0.1393 | 0.972 |
| 3 | `compositional_interaction_twin` | 0.5673 [0.5617, 0.5717] | 0.7690 | 0.0667 | 0.925 |
| 5 | `mlp` | 0.5685 [0.5631, 0.5731] | 0.7679 | 0.0281 | 0.915 |
| 6 | `mean` | 0.5994 [0.5943, 0.6051] | 0.7417 | 0.1662 | 0.993 |
| 7 | `knn` | 0.6283 [0.6242, 0.6335] | 0.7165 | 0.1573 | 0.992 |

前 5 名 CI 全部重叠 → **ID 上没有任何模型显著优于线性**。

### 右栏 · 机制泛化（OOD）

**ood_agent（未见基因，n=11748）**

| 排名 | 预测器 | RMSE [95% CI] | ECE | 覆盖@0.9 |
|---|---|---|---|---|
| 1 | `linear` | **0.5815** [0.5778, 0.5863] | 0.0243 | 0.909 |
| 2 | `compositional_twin` | 0.5852 [0.5814, 0.5898] | 0.0887 | 0.929 |
| 2 | `compositional_interaction_twin` | 0.5852 [0.5814, 0.5898] | 0.0479 | 0.865 |
| 4 | `mean` | 0.5872 [0.5838, 0.5918] | 0.1686 | 0.993 |
| 5 | `mlp` | 0.6376 [0.6337, 0.6416] | 0.0270 | 0.879 |
| 6 | `knn` | 0.6483 [0.6451, 0.6520] | 0.1611 | 0.995 |
| 7 | `virtual_twin` | 0.6751 [0.6706, 0.6795] | **0.7332** | **0.065** |

**ood_action（组合双扰动 held-out 对，n=14212）** ← 唯一 `should_generalize=true` 子集

| 排名 | 预测器 | RMSE [95% CI] | ECE | 覆盖@0.9 | flags |
|---|---|---|---|---|---|
| 1 | `linear` | **0.6266** [0.6218, 0.6310] | 0.0134 | 0.887 | — |
| 2 | `compositional_interaction_twin` | 0.6319 [0.6275, 0.6361] | **0.2125** | **1.000** | 疑似仅记忆 (Δ −0.0053) |
| 3 | `compositional_twin` | 0.6355 [0.6301, 0.6400] | 0.1965 | 0.997 | 疑似仅记忆 (Δ −0.0089) |
| 4 | `mlp` | 0.6361 [0.6317, 0.6396] | 0.0164 | 0.882 | — |
| 5 | `virtual_twin` | 0.6514 [0.6475, 0.6550] | 0.7055 | 0.097 | — |
| 6 | `mean` | 0.6577 [0.6528, 0.6617] | 0.1536 | 0.988 | — |
| 7 | `knn` | 0.6975 [0.6925, 0.7026] | 0.1424 | 0.985 | — |

**ood_neuro（未见批次，n=5912）**

| 排名 | 预测器 | RMSE [95% CI] | ECE | 覆盖@0.9 |
|---|---|---|---|---|
| 1 | `mlp` | **0.5992** [0.5920, 0.6061] | 0.0167 | 0.899 |
| 2 | `virtual_twin` | 0.6002 [0.5928, 0.6073] | 0.7237 | 0.075 |
| 3 | `compositional_twin` | 0.6026 [0.5951, 0.6097] | 0.1308 | 0.967 |
| 3 | `compositional_interaction_twin` | 0.6026 [0.5951, 0.6097] | 0.0574 | 0.914 |
| 5 | `linear` | 0.6028 [0.5953, 0.6101] | 0.0201 | 0.896 |
| 6 | `mean` | 0.6243 [0.6154, 0.6315] | 0.1607 | 0.991 |
| 7 | `knn` | 0.6548 [0.6463, 0.6614] | 0.1508 | 0.990 |

前 5 名 CI 全部重叠 → **ood_neuro 轴当前无判别力**。

### 双栏差额 = 机制证据？

| 子集 | 最优结构化孪生 | `linear` | 差额 | 判定 |
|---|---|---|---|---|
| ID（记忆） | 0.5673 | 0.5664 | **−0.0009**（孪生更差） | 无机制红利 |
| ood_agent | 0.5852 | 0.5815 | **−0.0037**（孪生更差） | 无机制红利 |
| ood_action | 0.6319 | 0.6266 | **−0.0053**（孪生更差） | 无机制红利 + flags 标记 |
| ood_neuro | 0.6026 | 0.6028 | +0.0002（CI 重叠） | 不显著 |

**结论（自 W31 起未被任何证据推翻）**：结构化组合先验在**四个子集全部未取得机制红利**。
差额一栏全为负或不显著 → 「机制泛化」目前**没有点估计层面的证据支持**。

---

## 2. 判别性指标复核：判据本身可靠吗？

`addendum_metric_discriminability`（核心 4 器 RMSE 点估计跨度 ÷ 平均 CI 半宽）：

| 子集 | 核心跨度 | 平均 CI 半宽 | **分辨率比值** | 可判据性 |
|---|---|---|---|---|
| id | 0.03304 | 0.005039 | **6.56** | 可判 |
| ood_agent | 0.005751 | 0.004080 | **1.41** | ⚠️ **近乎无分辨率** |
| ood_action | 0.03113 | 0.004440 | **7.01** | 可判 |
| ood_neuro | 0.02174 | 0.007408 | **2.93** | 边缘可判 |

**本周与这条的关系**：§3 的重裁**不依赖** RMSE 分辨率——它是纯粹的**判据口径核算**
（覆盖率是否落在预锁带内、ECE 用几档），因此不受钝刀问题影响。这是本周判决能站住的结构性原因。

---

## 3. 本周新证据槽 · P5-A2 判据审计：「四假说全过」重裁为「预锁判据不通过」

### 3.1 背景与被审对象

08-15 记录：P5-A2（`ProperBayesTwin`，torch SGLD + pymc NUTS）在**合成数据** spike 上
「H1–H4 四假说全过」，据此把 PC-3 的收口方向判为「真实」，并**挂起 R4 kill-switch**。
被审工件：`experiments/20260815-p5a2-spike.json`（1659 字节，8 个 stratum = 2 方法 × 4 子集）。

**预锁判据**（`experiments/20260814-P5-structural-estimator-proposal.md`，与路 A 同以保证公平比较）：

```
ood_action 覆盖@0.95 ∈ [0.93, 0.97]   且   ECE < 0.08
```

### 3.2 已落盘工件逐 stratum 复核（可直接复算，非二次跑批）

| stratum | 覆盖@0.95 | 预锁带 [0.93,0.97] | ECE(单档) | mono_corr(σ,τ) | σ_epi 均值 |
|---|---|---|---|---|---|
| `torch:id` | 0.9474 | ✅ 带内 | 0.0321 | **+0.657** | 1.004 |
| `torch:ood_action` | **1.0000** | ❌ 越上界 | **0.0500** | **+0.803** | 3.707 |
| `torch:ood_agent` | **1.0000** | ❌ 越上界 | **0.0500** | **+0.579** | 4.330 |
| `torch:ood_neuro` | 0.9880 | ❌ 越上界 | 0.0380 | **+0.765** | 1.309 |
| `pymc:id` | 0.9673 | ✅ 带内（贴边） | 0.0173 | **+0.651** | 0.269 |
| `pymc:ood_action` | 0.9975 | ❌ 越上界 | 0.0475 | **+0.869** | 8.139 |
| `pymc:ood_agent` | **1.0000** | ❌ 越上界 | **0.0500** | **+0.883** | 11.374 |
| `pymc:ood_neuro` | 0.8830 | ❌ **欠**覆盖 | 0.0860 | **+0.866** | 1.102 |

### 3.3 两处硬伤

**硬伤 A · 判据目标子集 `ood_action` 两个方法均越带 → 预锁判据直接不通过。**

- `torch:ood_action` 覆盖 = **1.0000**（区间覆盖一切，信息量为零）
- `pymc:ood_action` 覆盖 = **0.9975**（越上界 0.97）

→ 预锁判据的第一个合取项（覆盖 ∈ [0.93,0.97]）在**两个方法上都失败**。
按预锁口径，`P5A2_SYNTH_ACCEPTED = false`。08-15 的「H2 覆盖≈校准 / H3 ood 覆盖≥id」
是**事后放宽的替代表述**——「≥id」与「落在带内」不是同一个判据，前者对过覆盖免疫，后者不免疫。

**硬伤 B · 「ECE < 0.08」在饱和 stratum 上是伪通过（单档饱和地板）。**

`scripts/p5a2_bayes.py::ece()` 只评**单一名义档 0.95**。当区间完全饱和（实测覆盖 ≡ 1.0000）时：

```
ECE_单档[0.95]  = |1.0000 − 0.95| = 0.0500   ← 数学地板，低于 0.08 判据线 → 必然「通过」
ECE_canonical[0.5,0.8,0.9,0.95] = mean(|1−L|) = (0.5+0.2+0.1+0.05)/4 = 0.2125   ← 远高于任何判据线
```

**证据不是推测**：表中 `torch:ood_action`、`torch:ood_agent`、`pymc:ood_agent` 三处
**精确同时**出现 `覆盖 = 1.0000` 与 `ECE = 0.0500`。三处精确重合于理论地板值，
是**饱和签名**而非校准成绩。

→ 结论：**单档 ECE 判据是废判据**。其饱和地板 0.0500 < 0.08 判据线，
意味着一个完全退化的过宽区间可以合法通过。而 canonical 4 档口径的饱和地板 0.2125 ≫ 0.08，
天然把饱和挡在门外。**此后所有 ECE 判据必须声明档位口径，未声明者无效。**

### 3.4 什么**没有**被推翻：H1 单调性

`mono_corr(σ, τ)`（epistemic σ 与「到训练流形距离」τ 的相关）在**全部 8 个 stratum 为正**：
**+0.579 ~ +0.883**。这一指标**不受区间饱和污染**——它度量 σ 的*排序*是否随新颖度上升，
与区间绝对宽度无关。

→ **H1 是本周唯一站得住的正向信号，且它正是 PC-3 的要害**：
numpy-SGLD 在此处失败（`∂σ/∂τ ≈ 0`），proper-Bayesian 在此处成功（`∂σ/∂τ > 0` 由后验构造满足）。
σ_epi 均值随子集单调放大（torch: id 1.00 → ood_neuro 1.31 → ood_action 3.71 → ood_agent 4.33；
pymc: 0.27 → 1.10 → 8.14 → 11.37）也与之一致。

**但**：σ 的**排序正确**不等于 σ 的**尺度正确**。当前尺度**系统性过大**（→ 过覆盖 → 越带）。
这恰好是一个**新的、更精确的**问题陈述：从「σ 不随新颖度变化」（numpy-SGLD 的病）
变为「σ 随新颖度正确变化但整体偏大」（P5-A2 的病）。后者是**尺度标定问题**，前者是**结构缺陷**。

### 3.5 判决与代价

```
P5A2_SYNTH_ACCEPTED (预锁口径)      = false     ← 本周重裁
P5A2_as_logged_0815 (单档 ECE 口径) = true      ← 已作废的口径
H1 单调性 (不受饱和污染)             = 成立      ← 唯一保留的正向结论
适用域                              = 仅合成数据（真实 99k×217 未跑）
```

**对上周决策的追溯影响**：08-15 基于「四假说全过」**挂起了 R4 kill-switch**。
该挂起的证据基础现已被削弱到只剩 H1。
→ **本周处置**：R4 kill-switch **恢复生效**（OOD 区间一律标「不可信」，仅点估计参与判决）。
P5-A2 不因此关闭——H1 仍指向真实方向——但它从「已验证的收口路径」降级为
「**尺度未标定的候选路径，且仅有合成数据证据**」。

### 3.6 这条证据的性质：判据漂移，与 08-07 幻觉事故同源

08-07 曾发生「声称跑了 multiseed 重标定但零工件」的事故
（`experiments/20260807-RETRACTION-w32-multiseed-recalibration.md`，3 条结论 RETRACTED）。
本周这条**不是**幻觉——数字全部真实落盘、可复算。它是**另一种失真形式**：

| 失真形式 | 08-07 事故 | 本周（08-15 记录） |
|---|---|---|
| 数字真实性 | ❌ 无工件 | ✅ 工件真实 |
| 失真机制 | 凭空生成 | **判据被事后软化** |
| 检出方式 | `find` 工件 | **核对预锁判据原文 + 算指标地板** |
| 危害 | 结论虚假 | 结论「技术为真、判决为假」——**更难检出** |

→ **新增记账规则 R5**（写入项目铁律）：

> **R5 · 判据不可事后改写。** 任何 spike/实验的验收陈述必须**逐字引用**预锁判据原文
> （含区间上下界与指标档位口径），并逐项给出实测值与通过/不通过。
> 禁止用「≈校准」「≥基线」「显著改善」等**方向性表述**替代**带边界的预锁判据**。
> 任何含阈值的判据若未声明**指标口径**（如 ECE 档位），该判据即为无效判据。

---

## 4. 不确定度与校准审计（三种失效模式 + R4/R5 闸门）

### 4.1 失效模式 A · `virtual_twin` 全域灾难性过度自信（P3-1b，独立脚本已修，主基准未回写）

| 子集 | ECE | 覆盖@0.9 | 覆盖@0.95 | 点估计退化 vs ID |
|---|---|---|---|---|
| id | 0.7216 | 0.078 | 0.092 | — |
| ood_agent | 0.7332 | 0.065 | 0.077 | +19.04% |
| ood_action | 0.7055 | 0.097 | 0.115 | +14.87% |
| ood_neuro | 0.7237 | 0.075 | 0.089 | +5.84% |

名义 90% 区间只覆盖 6.5%–9.7% 真值 → 区间宽度被低估约一个数量级。
P3-1b 共形修复（q=30.2，ECE 0.71→0.14、覆盖 0.07→0.98）在独立脚本中跑通，**尚未回写主基准**
→ 主基准中它仍是失校准态，wetlab 标尺表按失校准处理。
**注意**：点估计退化全部远低于 50% → 原 R2 规则永不触发（已作废，见 wetlab §6.2②）。

### 4.2 失效模式 B · `ood_action` 区间饱和（P4-1 已跑·阴性）

`compositional_interaction_twin` 在 ood_action：ECE **0.2125** / 覆盖@0.9 **1.000** / 覆盖@0.95 **1.000**。
**0.2125 正是 canonical 4 档饱和地板** `mean(|1−[0.5,0.8,0.9,0.95]|)` —— 区间宽到覆盖一切，信息量为零。
P4-1（localized 共形）已证伪「换更细校准可治」：四子集 q 全退化回全局 41.26。

### 4.3 失效模式 C（本周新增）· **判据侧饱和伪通过**

前两种是**模型/区间**的失效；这一种是**判据本身**的失效（§3.3 硬伤 B）。
同一个饱和现象，在 4 档口径下显示为 ECE 0.2125（明显失败），在单档口径下显示为 0.0500（合法通过）。
→ 失效模式 C 的检出器 = **口径核算**（算指标在退化态的地板值，与判据线比较）。
这应成为所有新判据上线前的**必做步骤**。

### 4.4 R4 闸门（校准）现状

> **R4**：预测器若 `ECE > 0.15` 或 `|覆盖@0.9 − 0.90| > 0.05`，其区间**不得**用作证伪标尺。
> 仍可参与点估计排名，但须标注「区间不可信」。**ECE 一律用 canonical 4 档口径**（本周新增约束）。

| 预测器 | id | ood_agent | ood_action | ood_neuro |
|---|---|---|---|---|
| `linear` | ✅ | ✅ | ✅ | ✅ |
| `mlp` | ✅ | ✅ | ✅ | ✅ |
| `compositional_interaction_twin` | ✅ | ✅ | ❌ | ✅ |
| `compositional_twin` | ❌ | ✅ | ❌ | ❌ |
| `virtual_twin` | ❌ | ❌ | ❌ | ❌ |
| `knn` / `mean` | ❌ | ❌ | ❌ | ❌ |

**刺眼的事实（第 6 周未变）**：`linear` 是唯一四子集区间全部可用的预测器，同时是三个子集的最优点估计器。
→ 「孪生虽点估计不占优，但不确定度更可信」这条辩护路径**保持关闭**。在本基准上点估计质量与校准质量**正相关**，孪生两头都输。

**R4 kill-switch 状态**：08-15 因 P5-A2「阳性」而**挂起**；本周 §3 重裁后**恢复生效**。

### 4.5 反事实探针（`do-` 干预）

| 样本 | base 预测 | do(gain=1.6) | scale_ratio | do(action=other) |
|---|---|---|---|---|
| 32887 | 2.768 | 2.647 | 0.956 | **2.768**（= base） |
| 37845 | 2.855 | 2.734 | 0.958 | **2.855**（= base） |
| 856 | 2.745 | 2.623 | 0.956 | **2.745**（= base） |

- **增益轴**：scale_ratio ≈ 0.956–0.958 **< 1** → 调质增益上调 1.6× 反使响应略降。**乘法放大机制不存在**。
- **动作轴（镜像）**：`do(action=other)` 与 base **逐位相等** → mirror_k 退化为**恒等映射 1.000**。
  **模型的镜像轴是死的**——这是「虚拟镜像神经元」签名预测最薄弱的一环。
- ⚠️ 仅 3 行样本、无 bootstrap CI → 只作方向性判据，不得宣称显著。

---

## 5. Top-K 预测清单（按信息增益 / 不确定度排序）

| K | 目标 | 类型 | 当前状态 | 预期信息增益 | 判据（预锁） |
|---|---|---|---|---|---|
| 1 | **E2 镜像轴反向证伪** | 湿实验 | 模型预测 self/other 比值 = **1.000**（零效应） | **最高**：实测比值显著 ≠ 1 即直接证伪模型镜像轴 | 配对差值 SEM，≥8 只动物 |
| 2 | **P5-A2 真实数据收口** | 建模 | ⚠️ **合成阳性已重裁为判据不通过**；仅 H1 单调性保留 | 高：唯一未关闭的 PC-3 方向，但须先解决 σ **尺度**过大 | ood_action 覆盖@0.95 ∈ [0.93,0.97] **且** ECE(4 档) < 0.08 |
| 3 | **σ 尺度标定（P5-A2 新子问题）** | 建模 | 🆕 本周由 §3.4 析出 | 高：把「结构缺陷」降为「标定问题」后的直接后继 | 合成数据上 ood_action 覆盖先落入带内 |
| 4 | **W37 基准复现（已产出）** | 评测 | ✅ **已产出（08:53），但与 20260812 三段 sha256 冻结** | 低：确认非环境退化（已证：regress-check curr==prev） | 维持每周复现；关注冻结是否源于数据/代码未变 |
| 5 | **ood_agent 主指标换 pbRMSE** | 评测 | 分辨率仅 1.41，RMSE 为钝刀 | 中高：不换则该轴所有判据无效 | pbRMSE 分辨率比值 > 3 |
| 6 | **R4/R5 写入 `benchmark_ood.py`** | 工程 | 未启动（仍靠人工附录） | 中：把本周两条规则自动化，防再次漂移 | 基准报告自动输出 R4 表 + 判据口径声明 |
| 7 | **E7 闭环光遗传** | 湿实验 | 标尺不依赖失校准孪生 | 中：唯一能检验增益是否状态依赖 | ≥30 次闭环 trial，ΔF/F 均值±SEM |
| 8 | **E4 新调质外推** | 湿实验 | 三预测器 CI 全重叠 | **低（建议推迟）** | 需先提升该轴分辨率，否则浪费动物 |

**不确定度最高（= 最值得测）的轴仍是动作/镜像轴**：它同时具备「模型预测零效应」+「无 CI 支撑」+
「项目签名主张」三重属性，是唯一能一次性大幅改变项目可信度的实验。

---

## 6. 未解矛盾（TRIZ 分类）

### PC-1 · 物理矛盾（项目核心）：同一个 φ 既要记住分布，又要外推未知

- **表现**：四个子集上结构化孪生对 `linear` 的差额全为负或不显著（§1 差额表）。
- **状态**：**结构路径已耗尽**。P3-1（σ 解耦）/P4-2（插补）/P4-3（收缩）/P4-4（双通路）四类结构改造
  **全部「保判别力不补精度」**，无一在 ood_action 追平线性。
- **已定性根因**：误差主体是 **bias 非 variance**（τ²/SE² = 16.83，λ ≈ 0.94，收缩≈恒等）
  → 「改估计量」整类方向已证伪。差距 ~0.009 是**监督信号边界**（线性用全训练行含共扰动，
  组合孪生线性头仅单扰动行），非容量边界。
- **本周动作**：无新增（本周新证据槽在 PC-3/判据侧）。价值主张保持重定位为
  **可解释分解（φ+残差）+ 组间方差校准（var_ratio）**，不再做 φ 结构手术。

### PC-2 · 物理矛盾：判别力 vs 精度 vs 校准

- **表现**：修好 ood_agent 常数坍缩（var_ratio 1.07e-26 → 0.50/0.48）的代价是
  RMSE 0.5854 → 0.5887/0.5881 **且** ECE 0.0887 → 0.1490/0.1487。
- **已知陷阱**：armA 坍缩时 ECE 0.0887 是三臂**最好**的 → **坍缩会伪装成校准优势**。
  ECE 必须与 `var_ratio` 联合读。
- **本周新增关联**：§3 的失效模式 C 是这条陷阱的**判据层版本**——
  坍缩/饱和不仅能骗过 ECE 的*数值*，还能骗过 ECE 的*判据*。同一个病，两个层级。
- **状态**：未消解。三者联合帕累托前沿尚未刻画。

### PC-3 · 物理矛盾：区间要宽（保覆盖）vs 要窄（保可证伪）

- **表现**：两个极端同时存在于一份基准——`ood_action` 孪生区间饱和（ECE 0.2125 / 覆盖 1.000，宽到无信息）；
  `virtual_twin` 区间崩坏（ECE 0.73 / 覆盖 0.065，窄到无覆盖）。
- **已关闭的破法（全部实测阴性）**：
  - 校准层：P4-1 localized 共形 → 四子集 q 退化回全局 41.26（conformal i.i.d. 在 OOD 结构性失效）。
  - 估计器层·测试时重标定：路 A novelty/heteroscedastic 门控 → 新颖度在离散特征空间为**二值**；
    P5-C GMM 密度门控 → 217 维稀疏密度塌缩为二值 in/out。
    → **「按测试时协变量门控 epistemic」整类已排除**。
  - 训练期多样性注入：P5-B sample_weight + proper-NCL 双变体 → **共识塌缩**
    （去相关项 ∝(f_m−f̄) 随成员 MSE 收敛→0，无法自举多样性）。
  - 采样式后验：P5-A numpy-SGLD → 覆盖结构性可修（0.245→0.968 入带）但**点估计代价 2.4–6×**
    （var_ratio≈1e-27，均值近塌缩），ECE 下限 ~0.094，非可用预测器。
- **本周状态更新（§3）**：P5-A2 proper-Bayesian 的**合成阳性被重裁为判据不通过**。
  **但问题陈述被精化了**——这是本周 PC-3 唯一的真实进展：
  ```
  numpy-SGLD 的病：∂σ/∂τ ≈ 0        （σ 不随新颖度变化 → 结构缺陷）
  P5-A2  的病：∂σ/∂τ > 0 但 |σ| 过大  （排序正确、尺度过大 → 标定问题）
  ```
  **结构缺陷 → 标定问题**是一次真实的降级：后者有已知工具（温度/后验离散度/尺度先验），
  前者没有。这是把矛盾从「不知道往哪走」推进到「知道该调什么」。
- **破法定位（TRIZ）**：仍是**条件分离**，但作用点从「按新颖度分区间」（已证伪）
  移到「按后验离散度标定尺度」（H1 已提供正确的**序**，缺正确的**标度**）。
- **状态**：**原理性未解，R4 兜底恢复生效**。唯一未关闭方向 = P5-A2 σ 尺度标定 + 真实数据复现。

### TC-1 · 技术矛盾：`virtual_twin` 的多尺度先验只买到点估计

ID 上 RMSE 0.5671 排第 2（与 `linear` CI 重叠），但区间在四子集全废。
判定：先验换来拟合、没换来可信度。**优先级仍低**（不在主线）。

### TC-2 · 技术矛盾：判据要严格 vs 指标要有分辨率

- **表现**：ood_agent 分辨率比值 1.41，而多数判据要求分辨小数第 3 位。
- **临时破法**：引入**独立于该指标的机制诊断**作为第二证据源（P4-3 判决即靠此站住）。
- **本周新增反面案例**：§3 显示，当「严格判据」与「钝指标」冲突时，
  一个**危险的解法是软化判据**（08-15 即如此）。R5 就是为封死这条路而设。
- **根本破法**：ood_agent 主指标换 pbRMSE（Top-K #5）。**状态**：临时可用，根本未解。

---

## 7. 下一步（按判据，非按工作量）

| 优先级 | 任务 | 判据（跑前冻结，含口径） | 依赖 | 状态 |
|---|---|---|---|---|
| **P0** | **P5-A2 σ 尺度标定**（合成数据先行） | 合成 ood_action 覆盖@0.95 ∈ [0.93,0.97] **且** ECE(canonical 4 档) < 0.08；H1 mono_corr 保持 > 0.5 | §3 重裁（排序对、尺度大） | 🆕 本周析出 |
| **P1** | **W37 基准复现（已产出）** | 维持每周复现；若再次冻结须确认数据/代码未变（已证：regress-check curr==prev sha256） | 跑批环境 | ✅ 本周已产出（08:53，三段冻结） |
| **P2** | **R4 + R5 写入 `benchmark_ood.py`** | 基准报告自动输出：R4 可用性表 + 每条判据的口径声明（档位/边界） | 无 | 未启动（仍靠人工附录） |
| **P3** | P5-A2 真实数据（99k×217）+ conformal GATE_REPRO | 同 P0 判据，但在真实 Norman 上 | P0 先过 | 阻塞于 P0 |
| **P4** | ood_agent 主指标换 pbRMSE | pbRMSE 分辨率比值 > 3（现 RMSE 为 1.41） | 无 | 未启动 |
| **P5** | 湿实验 E7 → E2 → E5 → E3 | 见 `wetlab/2026-W33.md` §6.4 | 湿实验资源 + 伦理审批 | 未启动 |

**明确不做（负面清单，逐条有实测证据）**：
1. 任何「逆方差 / 加权 / 插补精化」变体 —— P4-3 已证伪（误差是 bias 非 variance）。
2. 任何「分 subset / localized 共形重标定」变体 —— P4-1 已证伪（四子集 q 退化回全局）。
3. 任何「测试时协变量密度/新颖度门控撑大 epistemic σ」变体 —— 路 A + P5-C 已证伪**整类**
   （离散特征空间新颖度二值化、217 维密度塌缩）。
4. 任何「同架构集成 + 多样性正则」变体 —— P5-B 双变体已证伪（共识塌缩，非调参可解）。
5. 任何「再加一层网络」式提容量方案 —— 掩盖机制缺失，违反项目硬约束。
6. **（本周新增）任何未声明指标口径的阈值判据** —— R5。

---

## 8. 诚实边界

1. **W37 跑批已产出（08:53），结果与 `20260812` 三段 sha256 逐位冻结。** 种子现为 `20260816-benchmark.json`
   （数值与 `20260812` 一致）。这是**结果稳定（冻结）**，不是「跑批未落地」——早先基于 0 字节 log 的
   「未产出」判断已证伪并更正。
2. **主基准连续第 5 次三段 sha256 逐位冻结**，主基准维度零建模增量。
3. **本周唯一实质新增（§3）是一条负向的方法论修正**：08-15 的「P5-A2 四假说全过」
   在预锁判据下**不成立**。数字本身真实无误——失真发生在**判据口径**层面。
4. **§3 的全部数字可从 `experiments/20260815-p5a2-spike.json`（已落盘，1659 字节）直接复算**，
   不依赖任何在跑任务。0.2125 与 0.0500 两个地板值是 `mean(|1−L|)` 的**算术恒等结果**，
   不是实验测量值。
5. **H1（σ 单调于流形距离，mono_corr +0.579~+0.883）是本周唯一保留的正向结论**，
   且**仅在合成数据上成立**。真实 99k×217 未跑。合成流形可能过于干净而致乐观。
6. **R4 kill-switch 恢复生效**（08-15 的挂起依据已被削弱到只剩 H1）。
   OOD 区间一律标「不可信」，仅点估计参与机制泛化判决。
7. **P5-A2 未被关闭，但已降级**：从「已验证收口路径」→「尺度未标定的候选路径（仅合成证据）」。
8. **反事实探针仅 3 行样本、无 CI** → 只作方向性判据。
9. **「疑似仅记忆」flag 仅在点估计层面成立**。ood_action 区间已饱和（覆盖 1.000），
   区间退化时不得声称「已证明模型只在记忆」。
10. **历史事故对照**：`experiments/20260807-RETRACTION-w32-multiseed-recalibration.md`（3 条结论 RETRACTED）
    是「无工件」型失真；本周是「判据软化」型失真。两者机制不同，检出方式不同（§3.6 表），
    但同属长程叙事后半段最易失真的区域。
11. **GOAI 节点如实记录**：今日（08-16）为初赛截止日；提交包已打包（92.98 MB，08-07）但**未提交**；
    GOAI 侧自 08-08 无新证据。本周报**不对提交结果作任何预测**。
12. **未外发、未发布、未向 GOAI 提交。**

---

## 9. 文献监测（无本周新增）

本周未执行新一轮检索。最近一轮为 `references/2026-08-12-literature.md`（8 方向 6 轮，去重后 9 篇）。
其两条可借鉴方法与本周结论的关系：

1. **VCBench 的 spread-error 认知校准探针**：与本周 §4.3「失效模式 C」高度互补——
   spread-error 度量「预测展布 vs 实际误差」的相关性，正是检出饱和/坍缩的诊断器。
   **建议并入 `verify_collapse`**，与本周的口径核算形成双检。
2. **Mechanisms Matter (Transportability) 的 CausalDGP + Vendi 多样性诊断**：
   仍可作**诊断**工具（监测区间饱和 / 预测多样性坍缩），
   但**不能**作为「修区间」的配方——P4-1 已证伪该前提（ID 校准不可转移到 OOD）。

方法论底座（外部证据，未变）：
- **架构谦逊**：Ahlmann-Eltze/Huber/Anders, *Nat Methods* 2025（DL 基础模型未稳定超越线性基线）。
- **可加天花板**：Systema, *Nat Biotech* 2025（perturbed mean 0.70 > GEARS 0.65 > scGPT 0.62）。
- **Decoder(Cov) 崩溃陷阱**：PerturBench, arXiv:2408.10609（拟合漂亮但 rank_average≈0.47 = 随机）。
- **诚实边界**：D-SPIN = *Cell* 2026（硬证据）；Chreode = arXiv preprint（**勿写已发表**）。

---

## 附 · 本报告引用的工件清单

| 工件 | 字节 | 用途 |
|---|---|---|
| `experiments/20260816-benchmark.json` | 40765 | **种子基准**：双栏总表 / 校准 / flags / 反事实（与 20260812 数值逐位冻结） |
| `experiments/20260816-benchmark.md` | 7233 | 种子基准可读版 |
| `experiments/20260816-regress-check.json` | 1074 | 回归守卫：三段 sha256 逐位冻结证明（curr==prev=47f8fcd…，连续第 6 次） |
| `experiments/20260815-p5a2-spike.json` | 1659 | **本周新证据槽**：P5-A2 合成 spike 8 stratum（被审对象，§3 全部数字来源） |
| `experiments/20260815-p5a2-torch.json` | 965 | P5-A2 torch 侧单跑记录 |
| `experiments/w36_p5a2_spike.log` | 1517 | P5-A2 spike 运行日志 |
| `code/model/compositional_p5a2_bayes.py` | 8569 | `ProperBayesTwin`（torch SGLD + pymc NUTS） |
| `scripts/p5a2_bayes.py` | 6308 | P5-A2 spike 跑批脚本（其 `ece()` 为单档 0.95 → §3.3 硬伤 B 根源） |
| `scripts/p5a2_readjudicate.py` | 12004 | **本周新增**：判据审计脚本（canonical 4 档重裁 + GATE_REPRO + 地板分析）；其输出 `20260816-p5a2-criterion-audit.json` **未产出**（PyMC NUTS 编译需 g++，后台采样未结束），§3 重裁改由已落盘 spike JSON 手工算术完成 |
| `experiments/20260816-p5a2-criterion-audit.json` | — | ⚠️ 审计脚本输出**未产出**（见上条）；§3 现依赖手工算术，非脚本产物 |
| `experiments/w37_benchmark_run_20260816.log` | 839 | W37 基准跑批日志（非空，**证明已产出**；此前 0 字节空文件已被覆盖） |
| `experiments/20260814-P5A-SGLD-closure.md` | 9476 | P5-A SGLD 结案（覆盖入带但点估计劣化 2.4–6×） |
| `experiments/20260814-P5B-NCL-closure.md` | — | P5-B proper-NCL 结案（共识塌缩，双阴性） |
| `experiments/20260814-P5C-GMM-closure.md` | — | P5-C GMM 结案（217 维密度塌缩为二值） |
| `experiments/20260814-P4A-closure.md` | — | 路 A 结案（novelty 二值 / σ_epi 弱负相关） |
| `experiments/20260814-P5-structural-estimator-proposal.md` | — | **预锁判据原文**（§3.1 引用的 [0.93,0.97] 与 ECE<0.08 出处） |
| `experiments/20260812-p44-dualpath.json` | 20214 | P4-4 四臂对照（PC-1 结构路径耗尽） |
| `experiments/20260812-p41-localized-conformal.json` | 10764 | P4-1 localized 共形（阴性，q 退化回全局） |
| `experiments/20260812-p31b-conformal-virtualtwin.json` | 3386 | P3-1b virtual_twin 共形修复（ECE 0.71→0.14，未回写主基准） |
| `experiments/20260810-p43-shrinkage.json` | 30760 | P4-3 三臂判决 + 收缩诊断（τ²/SE²=16.83） |
| `experiments/20260807-RETRACTION-w32-multiseed-recalibration.md` | — | 历史幻觉事故（§3.6 对照） |
| `wetlab/2026-W33.md` | 22682 | **本周湿实验方案**（7 协议 E1–E7 + §6 人工附录五小节） |
| `configs/benchmark_ood_norman_canonical.yaml` | 2241 | canonical 基准配置（ECE 4 档口径出处） |
| `data/processed/norman_cache.npz` | 191629995 | 真实 Norman 2019 缓存 |
| `submissions/GOAI初赛_CompositionalTwin_OOD外推.zip` | 92977330 | GOAI 提交包（08-07 打包，**未提交**） |

> 本周报由每周自动化（`automation-1785494084890`）生成，配套 `code/wetlab/export_protocol.py` 导出湿实验方案。
> 所有数值可通过上表工件回溯复现。**未外发、未发布。**
